{
  "manifest_version": 3,
  "name": "PagePilot Agent",
  "version": "1.1.1",
  "description": "AI-guided, step-by-step automation built on page-pilot. Given an instruction and a reference skill, decides one action at a time from the page's actual current state — survives page navigation, since task state lives in the background, not the page.",
  "permissions": ["storage", "scripting", "tabs"],
  "host_permissions": ["<all_urls>"],
  "background": {
    "service_worker": "extension/background/background.js",
    "type": "module"
  },
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "js": ["extension/content/content.js"],
      "run_at": "document_idle",
      "all_frames": false
    }
  ],
  "web_accessible_resources": [
    {
      "resources": ["src/*.js"],
      "matches": ["<all_urls>"]
    }
  ]
}
